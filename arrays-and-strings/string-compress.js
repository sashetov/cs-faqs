#!/bin/node
function compressStr(str) { /* Implement a method to perform basic string compression using the counts of repeated chars. If the 'compressed' string would not become smaller than the original string, your method should return the original string. You can assume the string has only uppercase and lowercase letters (a-z). EX: "aabcccccaaa"=>"a2b1c5a3".  */
}
