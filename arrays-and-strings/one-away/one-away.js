#!/bin/node 
function isOneOrZeroAway(s1, s2){/* There are three types of edits that can be performed on strings: insert a char, remove a char, or replace a char.  Given two strings, write a function to check i fhtey are one edit (or zero edits ) away.  EX: pale,   ple -> true.  pales, pale -> true.  pale,  bale -> true.  pale,  bake -> false. */
}
