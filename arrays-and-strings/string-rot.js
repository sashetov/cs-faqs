#!/bin/bash
function isSubstring(s1,s2){ /* Implement isSubstring to check if one word is substring of other*/
  return s1.indexOf(s2) != -1;
}
function stringRot(s1,s2){ /* Given two strings, s1 and s2, write code to check if s2 is a rotation of s1 using only one call to isSubstring. EX: "waterbottle" is a rotation of "erbottlewat" */
}
